# 开发日志 — 2026-07-31

今日目标
搭建 UI 系统骨架，实现回合指示和 AP 显示，为后续血条/飘字/胜负面板打好基础。

已完成

1. UI 架构搭建

设计并实现 BasePanel 抽象基类，提供统一的 Show()/Hide() 和事件生命周期钩子（OnShow/OnHide）。
实现 UIManager 单例，管理三个面板（探索/战斗/胜负）的切换，监听 BattleStart/BattleEnd 事件自动切换。
修复 EventBus 类型匹配问题：AddEventListener 是精确泛型匹配，lambda 注销必须用同一实例。将 lambda 提取为字段解决。
2. 战斗 HUD 核心信息

BattleUIPanel.OnShow 订阅 TurnStart 和 APChanged，OnHide 注销，无内存泄漏。
TurnIndicator：根据 IsCurrentUnitPlayer() 显示绿色"你的回合"或红色"敌人回合"。
APDisplay：通过 UnitAPManager.NotifyAPChanged() 驱动，零协程零轮询，AP 变更瞬间 UI 更新。
新增 E_EventType.APChanged 和 Struct/UnitAPData 数据结构。
3. 攻击体验优化

EnemyAI.DoAttack 和 PlayerAttackHandler.DoAttack 中加入 LookRotation 面朝目标逻辑，攻击瞬间角色转向目标。
4. 工程问题解决

Legacy Text 替代 TMP 以规避中文字体 SDF 生成问题（后续可统一迁移回 TMP）。
解决 Inspector 拖拽引用遗漏导致的 NullReferenceException。
待处理
UnitHPBar（World Space 角色头顶血条）
SkipTurnButton
UnitHPList（左侧队伍血条列表）
DamagePopupMgr（飘字对象池）
BattleResultPanel + 战败恢复

备注
项目脚本数量已超过 20 个，建议后续补充 Docs/DevLog/Architecture.md 架构文档，记录事件流和组件依赖关系。
